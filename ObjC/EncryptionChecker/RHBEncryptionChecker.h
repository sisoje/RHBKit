#import <Foundation/Foundation.h>


#define RHB_IS_ENCRYPTED [RHBEncryptionChecker isEncrypted:main]

int main (int, char **);

@interface RHBEncryptionChecker : NSObject
+ (BOOL)isEncrypted:(int(int, char **))main;
@end
